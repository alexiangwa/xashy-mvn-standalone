name: mymavenbuild
on: push

jobs:
  installmaven:
    runs-on: ubuntu-latest
    # permissions: 
    #     content: read
    #     package: write

    steps:
      - name: setup java and maven
        uses: actions/setup-java@v3
        with:
           distribution: 'temurin'
           java-version: '17'
           cache: 'maven'